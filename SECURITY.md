# Security note

A previous OpenAlex API key was committed to this public repository's Git history.
Treat that key as compromised: revoke/rotate it in OpenAlex.

Store the replacement only as the GitHub Actions repository secret:

`OPENALEX_API_KEY`

Do not commit secrets to files.
